/* Minimal stand-in for the real Vitis-generated platform header, used only for host-side -fsyntax-only checks (see VERIFICATION_GUIDE.md). Not for real Vitis builds. */
#ifndef PLATFORM_H
#define PLATFORM_H
void init_platform(void);
void cleanup_platform(void);
#endif
